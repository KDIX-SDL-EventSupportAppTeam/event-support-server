# 作業指示: 事前アンケートとアプリ公開ゲート（サーバー・DB 側）

- **日付:** 2026-08-17
- **状態:** 依頼（実装未着手）

## 仕様の正本

**`event-support-frontend/docs/orders/2026-08-17-pre-survey-and-app-access-gate.md`**

フロント・サーバー・DB を貫く仕様は上記 1 ファイルに集約している。本ファイルはサーバー側の作業範囲の索引のみ。

## サーバー・DB 側の作業範囲

| 節 | 内容 |
|----|------|
| §4 / §4' | 設計判断と、実効開放状態の算出ルール（`is_open` の唯一の定義） |
| §5 | DB: `event_app_access` 新規（13 → 14 テーブル）/ `survey_questions.answer_type` 追加 / `user_survey_answers` に `UNIQUE (user_id, event_id)` / 既存イベントの backfill |
| §6 | エンドポイント（公開 GET・organizer GET/PUT・admin GET・`pre-survey/questions`）、既存 survey 回答 API の締切チェックと upsert、監査ログ、ファイル配置、テスト観点 |
| §9 | 実装手順の 1〜4 がサーバー・DB 側。**4 まで終わってからフロントに着手する** |
| §10 | 未決定事項（回答済み判定 API の形、タイムゾーン、自動閉鎖の扱い）。着手前に結論を正本へ追記する |

## 前提として守るもの

- INSERT 前に SELECT で重複確認（[ADR 0001](../adrs/0001-sakura-proxy-error-masking.md)）
- ゲートの書き込みは `organizer` のみ。`manager` / `viewer` は読み取り専用
- 実効開放状態はサーバーが算出する（クライアントの時計を信用しない）
- 完了後に [AGENTS.md](../../AGENTS.md) のエンドポイント表とテーブル数を更新する
