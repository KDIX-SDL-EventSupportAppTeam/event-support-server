# さくらDB接続 WebAPIプロキシ実装

| 項目 | 内容 |
|------|------|
| 日付 | 2026-06-09 |
| 状態 | 完了 |
| 優先度 | 高（これが完了するまで本番DBに接続できない） |
| 担当 | Cursor |
| 影響範囲 | `src/db/` `src/scripts/` `src/config.ts` `src/app.ts` `src/index.ts` `src/types/fastify.d.ts` `package.json` `.env.example` |

---

## 背景と目的

本プロジェクトのDBはさくらインターネット レンタルサーバー スタンダードプランのMySQLを使用する。

さくらのStandardプランはセキュリティ上の理由から **さくらのサーバー内からしかMySQLに接続できない**（外部サーバーからのMySQL直接接続は不可）。Cloud RunからMySQLの3306ポートへ直接接続を試みたが、さくらのネットワーク境界でサイレントドロップされることを確認済み。

**解決策**: さくらサーバー上にHTTPラッパーAPI（プロキシ）を設置し、Cloud RunはHTTPS経由でそのAPIを呼び出す。HTTPSは海外IPからでも到達可能なため、この制限を回避できる。

```
【現状・不可】
Cloud Run ──── MySQL:3306 ──→ ❌ さくらのネットワーク境界でブロック

【実装後・可】
Cloud Run ──── HTTPS:443 ──→ さくら上のラッパーAPI ──── MySQL（ローカル）──→ ✅
```

ラッパーAPI本体はさくら上に先生が設置する。**本タスクではCloud Run側の実装と、ローカルでテストするためのモックサーバーを作成する**。テストが通ったらモックスクリプトを先生に渡す。

---

## 完了条件

- [x] `npm run proxy:mock` でモックサーバーが起動する
- [x] `SAKURA_PROXY_URL=http://localhost:3001 npm run dev` でFastifyがプロキシ経由でDockerMySQLに接続できる
- [x] `SAKURA_PROXY_URL` が未設定のとき、従来どおりmysql2で直接接続できる（ローカル開発の後方互換）
- [x] TypeScriptのビルドエラーがない（`npm run build`）
- [x] 既存のルートファイル（`auth.ts` `booths.ts` など）の変更が不要であること

---

## 対象外

- さくら上のラッパーAPI本体の実装（先生が担当）
- WebSocket実装（Issue #8・別タスク）
- 運営CRUD実装（Issue #8・別タスク）

---

## ラッパーAPI仕様（両者の契約）

先生がさくら上に設置するAPIと、こちらのHTTPクライアントが通信する仕様。

```
エンドポイント : POST /query
認証ヘッダー  : X-Proxy-Key: <SAKURA_PROXY_KEY と同じ値>
Content-Type  : application/json

リクエストBody:
{
  "sql":    "SELECT * FROM users WHERE event_id = ? LIMIT 1",
  "params": ["uuid-here"]
}

成功レスポンス 200（SELECT 系）:
{
  "rows":         [ { "id": "...", "email": "..." } ],
  "affectedRows": 0,
  "insertId":     null
}

成功レスポンス 200（INSERT / UPDATE / DELETE 系）:
{
  "rows":         [],
  "affectedRows": 1,
  "insertId":     null
}

エラーレスポンス:
401 → { "error": "Unauthorized" }
400 → { "error": "Bad Request: sql(string) と params(array) が必要です" }
500 → { "error": "Internal Server Error" }
```

---

## 実装指示

### 方針

- `app.db.query` / `app.db.execute` のインターフェースを変えない → **既存のルートファイルは一切変更しない**
- `SAKURA_PROXY_URL` が設定されていればHTTPプロキシを使い、なければmysql2直接接続（ローカル開発の後方互換を維持）
- 新規ファイルは5つ、変更ファイルは5つ

---

### 新規作成: `src/db/client.ts`

mysql2の `Pool` とHTTPプロキシ共通のインターフェース。`fastify.d.ts` はこの型を使う。

```typescript
/**
 * DbPool（mysql2）と HttpProxy の共通インターフェース。
 * routes は app.db.query / app.db.execute のみを使うため、この2メソッドだけ定義する。
 */
export interface DbClient {
  query(sql: string, params?: unknown[]): Promise<[unknown, unknown]>
  execute(sql: string, params?: unknown[]): Promise<[unknown, unknown]>
  end(): Promise<void>
}
```

---

### 新規作成: `src/db/http-proxy.ts`

さくら上のラッパーAPIをHTTP経由で呼ぶ、mysql2互換クライアント。

```typescript
import type { DbClient } from './client.js'

/**
 * さくら上のラッパーAPIをHTTP経由で呼び出す、mysql2互換クライアント。
 * app.db.query / app.db.execute のインターフェースはそのままなので、
 * routes 側を一切変更せずに切り替えられる。
 */
export function createHttpProxy(baseUrl: string, apiKey: string): DbClient {
  const headers = {
    'Content-Type': 'application/json',
    'X-Proxy-Key':  apiKey,
  } as const

  async function callProxy(sql: string, params: unknown[]) {
    const res = await fetch(`${baseUrl}/query`, {
      method: 'POST',
      headers,
      body: JSON.stringify({ sql, params }),
    })
    if (!res.ok) {
      const body = await res.json().catch(() => ({ error: `HTTP ${res.status}` }))
      throw new Error(
        `[sakura-proxy] ${res.status}: ${(body as { error?: string }).error ?? 'error'}`,
      )
    }
    return res.json() as Promise<{
      rows: unknown[]
      affectedRows: number
      insertId: number | null
    }>
  }

  return {
    async query(sql, params = []) {
      const data = await callProxy(sql, params)
      return [data.rows, []]
    },
    async execute(sql, params = []) {
      const data = await callProxy(sql, params)
      return [{ affectedRows: data.affectedRows, insertId: data.insertId }, []]
    },
    async end() {
      // HTTP クライアントはプールを持たないため何もしない
    },
  }
}
```

---

### 新規作成: `src/scripts/sakura-proxy-mock.ts`

ローカルでさくら側ラッパーAPIをシミュレートするモックサーバー。  
**テスト通過後にそのまま先生へ渡す**（先生のさくらサーバー上で動かす）。

```typescript
/**
 * sakura-proxy-mock.ts
 *
 * 目的:
 *   さくら側のラッパーAPIをローカルで再現し、event-support-server の
 *   http-proxy.ts が正しく動くかをローカルで検証する。
 *   テストが通ったら、このファイルをそのまま先生に渡してさくら上に設置してもらう。
 *
 * 起動:
 *   npm run proxy:mock
 *   （.env に SAKURA_PROXY_KEY と DATABASE_URL が必要）
 *
 * さくら設置後のエンドポイント例:
 *   https://example.sakura.ne.jp/proxy/query
 */
import 'dotenv/config'
import { createServer } from 'node:http'
import type { IncomingMessage, ServerResponse } from 'node:http'
import mysql from 'mysql2/promise'
import { parseMysqlUrl } from '../db/parse-mysql-url.js'

// ── 環境変数 ────────────────────────────────────────────────────────────
const PROXY_KEY    = process.env.SAKURA_PROXY_KEY ?? ''
const PROXY_PORT   = Number(process.env.PROXY_PORT ?? 3001)
const DATABASE_URL = process.env.DATABASE_URL ?? ''

if (!PROXY_KEY)    { console.error('[proxy-mock] SAKURA_PROXY_KEY が未設定です'); process.exit(1) }
if (!DATABASE_URL) { console.error('[proxy-mock] DATABASE_URL が未設定です');    process.exit(1) }

// ── DB接続（ローカル Docker MySQL）──────────────────────────────────────
const pool = mysql.createPool({
  ...parseMysqlUrl(DATABASE_URL),
  waitForConnections: true,
  connectionLimit: 5,
  timezone: 'Z',
  dateStrings: true,
})

// ── ユーティリティ ──────────────────────────────────────────────────────
function readBody(req: IncomingMessage): Promise<string> {
  return new Promise((resolve, reject) => {
    let body = ''
    req.on('data', (chunk: Buffer) => { body += chunk.toString() })
    req.on('end',  () => resolve(body))
    req.on('error', reject)
  })
}

function sendJson(res: ServerResponse, status: number, data: unknown): void {
  const body = JSON.stringify(data)
  res.writeHead(status, {
    'Content-Type':   'application/json',
    'Content-Length': Buffer.byteLength(body),
  })
  res.end(body)
}

// ── サーバー ────────────────────────────────────────────────────────────
const server = createServer(async (req, res) => {
  // POST /query のみ受け付ける
  if (req.method !== 'POST' || req.url !== '/query') {
    return sendJson(res, 404, { error: 'Not Found' })
  }

  // 認証
  if (req.headers['x-proxy-key'] !== PROXY_KEY) {
    return sendJson(res, 401, { error: 'Unauthorized' })
  }

  try {
    const raw  = await readBody(req)
    const body = JSON.parse(raw) as { sql?: unknown; params?: unknown }

    if (typeof body.sql !== 'string' || !Array.isArray(body.params)) {
      return sendJson(res, 400, {
        error: 'Bad Request: sql(string) と params(array) が必要です',
      })
    }

    const [result] = await pool.execute(body.sql, body.params)

    if (Array.isArray(result)) {
      // SELECT 系: rows に結果
      return sendJson(res, 200, { rows: result, affectedRows: 0, insertId: null })
    } else {
      // INSERT / UPDATE / DELETE 系
      const r = result as { affectedRows: number; insertId: number }
      return sendJson(res, 200, {
        rows: [],
        affectedRows: r.affectedRows,
        insertId:     r.insertId ?? null,
      })
    }
  } catch (e) {
    console.error('[proxy-mock] ERROR:', e instanceof Error ? e.message : e)
    return sendJson(res, 500, { error: 'Internal Server Error' })
  }
})

server.listen(PROXY_PORT, () => {
  const safeUrl = DATABASE_URL.replace(/:[^:@/]+@/, ':***@')
  console.log(`[proxy-mock] http://localhost:${PROXY_PORT} で起動中`)
  console.log(`[proxy-mock] DB: ${safeUrl}`)
  console.log(`[proxy-mock] Key prefix: ${PROXY_KEY.slice(0, 4)}...`)
})

// Graceful shutdown
const shutdown = () => pool.end().then(() => server.close(() => process.exit(0)))
process.on('SIGTERM', shutdown)
process.on('SIGINT',  shutdown)
```

---

### 変更: `src/db/pool.ts`

戻り値の型を `DbClient` に変更する。`DbPool` 型の export は削除する。

```typescript
import mysql from 'mysql2/promise'
import type { AppConfig } from '../config.js'
import { parseMysqlUrl } from './parse-mysql-url.js'
import type { DbClient } from './client.js'

export function createPool(config: AppConfig): DbClient {
  const pool = mysql.createPool({
    ...parseMysqlUrl(config.databaseUrl),
    waitForConnections: true,
    connectionLimit: 10,
    timezone: 'Z',
    dateStrings: true,
  })
  return {
    query:   (sql, params = []) => pool.query(sql, params)   as Promise<[unknown, unknown]>,
    execute: (sql, params = []) => pool.execute(sql, params) as Promise<[unknown, unknown]>,
    end:     () => pool.end(),
  }
}

// DbPool 型は削除（DbClient に統一）
```

---

### 変更: `src/config.ts`

- `DATABASE_URL` を optional に変更（プロキシ使用時は不要のため）
- `SAKURA_PROXY_URL` / `SAKURA_PROXY_KEY` を追加
- 両方未設定の場合は起動時エラー

```typescript
import 'dotenv/config'
import { z } from 'zod'

const envSchema = z.object({
  PORT:             z.coerce.number().default(3000),
  DATABASE_URL:     z.string().optional().default(''),   // optional に変更
  JWT_SECRET:       z.string().min(1),
  WEBHOOK_API_KEY:  z.string().optional().default(''),
  RECOMMENDER_URL:  z.string().optional().default(''),
  CORS_ORIGIN:      z.string().default('http://localhost:5173'),
  SAKURA_PROXY_URL: z.string().optional().default(''),   // 新規追加
  SAKURA_PROXY_KEY: z.string().optional().default(''),   // 新規追加
})

export type AppConfig = {
  port:           number
  databaseUrl:    string
  jwtSecret:      string
  webhookApiKey:  string
  recommenderUrl: string
  corsOrigin:     string
  sakuraProxyUrl: string  // 新規追加
  sakuraProxyKey: string  // 新規追加
}

export function loadConfig(): AppConfig {
  const parsed = envSchema.safeParse(process.env)
  if (!parsed.success) {
    throw new Error(`Invalid env: ${JSON.stringify(parsed.error.flatten().fieldErrors)}`)
  }
  const e = parsed.data

  // DATABASE_URL か SAKURA_PROXY_URL のどちらかは必須
  if (!e.DATABASE_URL && !e.SAKURA_PROXY_URL) {
    throw new Error('DATABASE_URL か SAKURA_PROXY_URL のどちらかは必須です')
  }

  return {
    port:           e.PORT,
    databaseUrl:    e.DATABASE_URL,
    jwtSecret:      e.JWT_SECRET,
    webhookApiKey:  e.WEBHOOK_API_KEY,
    recommenderUrl: e.RECOMMENDER_URL,
    corsOrigin:     e.CORS_ORIGIN,
    sakuraProxyUrl: e.SAKURA_PROXY_URL,
    sakuraProxyKey: e.SAKURA_PROXY_KEY,
  }
}
```

---

### 変更: `src/app.ts`

`DbPool` → `DbClient` に変更する（2箇所のみ）。

```typescript
// 変更前（2行）
import type { DbPool } from './db/pool.js'
export async function buildApp(config: AppConfig, pool: DbPool) {

// 変更後（2行）
import type { DbClient } from './db/client.js'
export async function buildApp(config: AppConfig, db: DbClient) {
```

`app.decorate('db', pool)` も `app.decorate('db', db)` に変更する。  
他の行は変更しない。

---

### 変更: `src/index.ts`

条件分岐でDB初期化を切り替える。

```typescript
import 'dotenv/config'
import { loadConfig }      from './config.js'
import { createPool }      from './db/pool.js'
import { createHttpProxy } from './db/http-proxy.js'
import { buildApp }        from './app.js'

async function main() {
  const config = loadConfig()

  // SAKURA_PROXY_URL が設定されていれば HTTP プロキシ、なければ mysql2 直接接続
  const db = config.sakuraProxyUrl
    ? createHttpProxy(config.sakuraProxyUrl, config.sakuraProxyKey)
    : createPool(config)

  const app = await buildApp(config, db)
  await app.listen({ port: config.port, host: '0.0.0.0' })
}

main().catch((e) => { console.error(e); process.exit(1) })
```

---

### 変更: `src/types/fastify.d.ts`

`DbPool` を `DbClient` に変更する。

```typescript
import type { AppConfig }  from '../config.js'
import type { DbClient }   from '../db/client.js'
import type { JwtPayload } from '../lib/jwt.js'

declare module 'fastify' {
  interface FastifyInstance {
    config: AppConfig
    db: DbClient
  }
  interface FastifyRequest {
    jwtUser?: JwtPayload
  }
}
```

---

### 変更: `package.json`

`scripts` に1行追加する。

```json
"proxy:mock": "tsx src/scripts/sakura-proxy-mock.ts"
```

既存スクリプトの順序は変えない。`"db:check"` の直前に追加するとよい。

---

### 変更: `.env.example`

ファイル末尾に以下を追記する。

```bash
# --- さくらプロキシ（本番DB接続用） -----------------------------------
# さくら上のラッパーAPIのURL（設定時は DATABASE_URL より優先される）
# 例: https://example.sakura.ne.jp/proxy
SAKURA_PROXY_URL=

# プロキシ認証キー（ラッパーAPI側と同じ値を設定する）
# 生成: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
SAKURA_PROXY_KEY=
```

---

## 実装後のローカルテスト手順

実装が完了したら、以下の順で動作確認を行う。

```bash
# 1. Docker MySQL 起動・スキーマ適用（未済の場合）
docker compose up -d mysql
npm run db:migrate
npm run db:seed

# 2. .env に SAKURA_PROXY_KEY を追記（任意の文字列でよい）
#    例: SAKURA_PROXY_KEY=dev-test-key-local

# 3. ビルドエラーがないことを確認
npm run build

# 4. ターミナルA: モックサーバーを起動
npm run proxy:mock
# 期待ログ:
#   [proxy-mock] http://localhost:3001 で起動中
#   [proxy-mock] DB: mysql://app:***@127.0.0.1:3306/event_support
#   [proxy-mock] Key prefix: dev-...

# 5. ターミナルB: Fastify をプロキシ経由で起動
SAKURA_PROXY_URL=http://localhost:3001 npm run dev
# 期待: DATABASE_URL の代わりにプロキシを使って起動

# 6. 疎通確認
curl http://localhost:3000/health
# 期待: { "ok": true, ... }

# 7. 認証エンドポイント確認（seed データのメールアドレスとパスワードを使う）
curl -X POST http://localhost:3000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"event_id":"<seed のイベントID>","email":"dev@example.com","password":"password123"}'
# 期待: { "ok": true, "data": { "token": "..." } }

# 8. 後方互換の確認（mysql2 直接接続が壊れていないか）
npm run dev   # SAKURA_PROXY_URL なしで起動
curl http://localhost:3000/health
```

---

## 注意事項

- `src/routes/v1/` 配下のファイルは**一切変更しない**
- `DbPool` 型は削除するため、他のファイルで `import type { DbPool }` していないか確認する（現状は `app.ts` と `fastify.d.ts` のみ）
- `SAKURA_PROXY_KEY` は推測されにくいランダム文字列を使う（本番では Secret Manager に登録する）
- モックサーバーはHTTPのため、ローカルテスト専用。本番さくら上では必ずHTTPS経由で呼ぶ

---

## 先生への引き渡し手順（テスト完了後）

1. `src/scripts/sakura-proxy-mock.ts` を先生に渡す
2. 先生のさくらサーバー上の公開パスを確認する（例: `https://xxxxx.sakura.ne.jp/proxy`）
3. Cloud Run に以下の環境変数を設定する
   - `SAKURA_PROXY_URL=https://xxxxx.sakura.ne.jp/proxy`
   - `SAKURA_PROXY_KEY=<先生と共有したキー>` → Secret Manager に登録
4. `DATABASE_URL` は Cloud Run から削除してよい（Secret Manager からも削除可）
5. Cloud Run を再デプロイして本番接続を確認する

---

## 実施結果（2026-06-09）

- ブランチ `feature/sakura-db-proxy` で Cloud Run 側クライアント・ローカルモックを実装
- ローカル検証: Docker MySQL + `npm run proxy:mock` + `SAKURA_PROXY_URL=http://localhost:3001 npm run dev` でログイン成功
- `src/routes/v1/` は未変更。`npm run build` 成功
- 残作業: さくら上のラッパー API 本体設置（先生）、Cloud Run への `SAKURA_PROXY_URL` / `SAKURA_PROXY_KEY` 設定
