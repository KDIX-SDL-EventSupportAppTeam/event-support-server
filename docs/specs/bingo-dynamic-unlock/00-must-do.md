---
状態: 確定
最終更新: 2026-08-25
---

# 絶対にやることリスト

**イベントは 2026-10-16 の1日だけ。当日のデータは二度と取り直せない。**
実装が終わってから思い出すのでは間に合わない項目を、先に固定しておく。

実装フェーズに入ったら、このファイルのチェックを埋めながら進める。
判定条件の詳細は [10-testing/acceptance-criteria.md](10-testing/acceptance-criteria.md)。

**2026-08-25 時点の消化状況**: 完了 13 / 部分 8 / 未着手 9（全30項目）。
実装の記録は [docs/tests/runs/2026-08-25-bingo-dynamic-unlock.md](../../tests/runs/2026-08-25-bingo-dynamic-unlock.md)。
`△` は「実装は入っているが、テストの網羅性や他システム側での確認が未了」の意味。

## テスト

- [x] **段階解放ロジックを DB・通信に触れない純関数として切り出す。**
      あとから分離するのは非常に大変で、最初から分けるしかない
      → `unlockPairs.ts` / `phases.ts` / `lines.ts`
- [x] 中央4マスの埋まり順を**総当たり（65通り）**でテストする
      → `tests/unit/bingo-unlock-pairs.test.ts`
- [x] 「起きてはいけないこと」を明示的にテストする（対応しないマスが開いていないこと）
- [ ] △ 解放の冪等性テスト（同一ペアで並行2回実行しても解放1回・ログ重複なし）
      → 逐次の二重実行は担保。**真の同時実行はトランザクション無しでは塞げず未解決**
- [ ] △ 推薦サービスのタイムアウト／500 時にも解放が必ず成功することのテスト
      → 実装は例外を上げずフォールバックへ。専用テストの網羅性は要確認
- [x] 自己修復が**解放3回ぶんすべて**で動くことのテスト
      → 1回目（2マス）/ 2回目（4マス）/ 3回目（6マス）の3パターンで、
      `healUnlockedCardIfNeeded` が全マスを埋め `strategy='SELF_HEAL'` で記録し、
      二重実行しても `recommendation_scores` が重複しないことを固定
      （`tests/unit/bingo-unlock.test.ts`「自己修復が解放3回ぶんすべてで動く（#92）」）
- [x] **カード取得 API が見えないマスの中身を返さない**ことのテスト

## 実機・負荷

- [ ] **実機リハーサル**（研究室メンバー10名規模で、実際にスマホで QR を読んで一周する）
      → [10-testing/rehearsal-plan.md](10-testing/rehearsal-plan.md)
- [ ] **負荷試験**（想定の3倍 = 1分あたり30チェックイン）。
      解放1回の所要時間・SQL 往復回数・WebSocket 接続の安定性を測る
      → **SQL 往復数はローカルで計測済み（解放3回目で33往復）。1往復50msなら約1.65秒。**
        さくらプロキシ経由の実測が必要
- [ ] 負荷試験の結果で「Cloud Run 1インスタンスで足りるか」を判断する。
      足りなければ socket.io のアダプタ方式導入（半日）を予備日程に確保する
- [ ] リハーサル用のイベントを**本番とは別に作る**（本番データを汚さない）

## フロント

- [x] **解放演出の再生済みフラグを、解放イベントごとに独立して持つ。**
      1回きり前提の実装（localStorage の単一フラグ）を残さない
      → `pair_key` 単位の sessionStorage フラグ。サーバーが `unlocked_pairs` を返す
- [x] 推薦欄 UI（`CheckInRecommendView`）を削除する
- [x] 推薦理由文の表示 UI を削除する（**データの記録は残す**）
- [x] ガチャ・アワード投票を「準備中」表示にし、旧 Flask 呼び出しコードを削除する

## データ

- [x] `recommendations` テーブルを DROP し、**既存データも削除する**
- [ ] △ `recommendation_scores` に**候補全件**を記録する（推薦されなかったものが分母になる）
      → 解放時は全件記録。**事前推薦マスだけは選ばれた1件のみ**で D-10 に未達
- [x] `interest_match` と `attributes` を**推薦時点の値で凍結**して保存する
- [x] `card_unlock_events` に `phase` / `decision_table_size` / `global_checkin_count` を必ず入れる
- [x] カード外訪問を `cell_id = NULL` で必ず記録する（対照群になる）
- [ ] △ 事前アンケート回答が `user_survey_answers` に入り、分析側が決定表として読めること
      → 保存は動作確認済み。**`event-support-analytics` 側での読み取り確認は未実施**

## 当日の監視

- [ ] △ **評価回収率をリアルタイムで見られるようにする。**
      推薦手法が成立するかを左右する最重要指標
      → 管理ダッシュボード API に実装済み。**画面表示は未確認**
- [ ] △ 現在のフェーズと、次のしきい値までの残り件数を表示する → API 実装済み
- [ ] △ 解放の到達人数（1回目 / 2回目 / 3回目）を表示する
      → API 実装済み。**算出式が本仕様書に無く、累計ペア数 1 / 3 / 6 以上として実装。要確認**
- [ ] △ 直近30分のフォールバック率を表示する（推薦エンジンの障害検知）→ API 実装済み

## 運用

- [x] マイグレーションは**事前アンケート側とまとめて1回**で実行する
      → `09_bingo_staged_unlock.sql` に統合済み
- [ ] ロールバック手順を書く
- [ ] 当日トラブル時の運営向け手引きを書く
- [ ] △ 出展者・運営スタッフのアカウントを分析から除外できる状態にしておく
      → 集計クエリは `role='participant'` で除外済み。分析側の方針は未確認

## 本番投入前に必ず判断すること

実装の過程で新たに判断が必要になった事項。

- [ ] **推薦エンジン（`event-support-recommend`）を作り切るか。**
      未着手で、現在はフォールバックのみが動作している。
      間に合わない場合の代替（SIMILARITY 止まりで走らせる等）を先に決める
- [ ] **本番 DB へのマイグレーション適用タイミング。**
      旧版の 09 は本番未適用。作り直しで進める方針だが、実行はこのフェーズになるまで行わない
- [ ] サーバーの待ち受けをデュアルスタック（`::`）にするか（開発時の `localhost` 200ms 問題の根治）
- [ ] アプリ公開ゲートの適用範囲。現在は `/home` のみで、
      チェックインやブース一覧は直接 URL で開ける
