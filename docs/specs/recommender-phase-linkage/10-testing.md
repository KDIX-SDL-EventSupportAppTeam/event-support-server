---
状態: 確定
最終更新: 2026-09-01
---

# テスト項目

方針は [rules/testing.md](../../rules/testing.md) に従う。
**DB に依存しない部分は純関数として切り出し、単体で確かめる。**

## 1. 中継エンドポイント

- [ ] T-1 推薦エンジンが 200 を返す → `available: true` と `state` がそのまま入る
- [ ] T-2 `RECOMMENDER_URL` が空 → `available: false` / `reason: UNCONFIGURED`、**200 で返る**
- [ ] T-3 推薦が 401 → `reason: UNAUTHORIZED`
- [ ] T-4 推薦が接続不能・タイムアウト → `reason: UNREACHABLE`
- [ ] T-5 推薦が JSON でない応答 → `reason: BAD_RESPONSE`
- [ ] T-6 **どのケースでも 500 を返さない**
- [ ] T-7 `X-Ops-Token` ヘッダが送られている（`Authorization` は送らない）
- [ ] T-8 **応答にもログにもトークンの値が出ない**
- [ ] T-9 10秒以内の連続呼び出しで、推薦エンジンへのリクエストが1回だけ
- [ ] T-10 10秒経過後は再取得する
- [ ] T-11 参加者ロールのトークンでは 403（運営のみ）

## 2. ダッシュボードの変更

- [ ] T-12 応答に `bingo.recommender.current_phase` が**含まれない**
- [ ] T-13 `rating_collection_rate` / `unlocks` / `fallback_rate_last_30min` は従来どおり返る
- [ ] T-14 推薦エンジンが落ちていてもダッシュボードは 200 で全項目を返す
- [ ] T-15 `determinePhase` が解放処理側では従来どおり使われている（**関数を消していない**）

## 3. 解放到達人数

- [x] T-16 累計ペア数 0 のカードはどの回にも数えられない
- [x] T-17 累計ペア数 1 → 1回目のみ
- [x] T-18 累計ペア数 3 → 1回目・2回目
- [x] T-19 累計ペア数 6 → 1回目・2回目・3回目
- [x] T-20 **`pair_key = 'PRESURVEY'` が数えられない**
- [x] T-21 出展者・運営（`role <> 'participant'`）のカードが数えられない

## 4. 推薦レスポンスの契約

- [ ] T-22 `scores[].rank` を返す応答で `rank_in_event` が **NULL にならず記録される**
- [ ] T-23 `scores[].rank_in_event` だけを返す応答（旧実装）でも記録される（後方互換）
- [ ] T-24 両方ある場合は `rank` を優先する
- [ ] T-25 どちらも無い場合は NULL で記録し、解放は成功する
- [ ] T-26 `assigned[]` に `score` / `rank` があっても無くても割当が成立する

## 5. 回帰（既存の不変条件を壊していないこと）

- [ ] T-27 推薦エンジンのタイムアウト・500 でも**解放は必ず成功する**
- [ ] T-28 `card_unlock_events.phase` には推薦エンジンが返した値がそのまま入る
      （**サーバーが上書きしない**）
- [ ] T-29 推薦が使えないときの `phase` はフォールバックの値になる（従来どおり）

## 事前検証（本番前に必ず1回）

- [ ] V-1 推薦エンジンをデプロイした状態で、運営ダッシュボードにフェーズが表示される
- [ ] V-2 推薦エンジンを止めると「取得不能」と出る（**数字が消えるだけにならない**）
- [ ] V-3 トークンを間違えた状態で「認証エラー」と区別して出る
