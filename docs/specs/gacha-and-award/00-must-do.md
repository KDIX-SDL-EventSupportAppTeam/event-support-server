---
状態: 確定
最終更新: 2026-08-27
---

# 絶対にやること（ガチャコイン）

イベントは **2026-10-16 の1日限り**で、当日のデータは取り直せない。
実装が終わってから思い出すのでは間に合わない項目を、先に固定しておく。

## 決定済み（2026-08-27）

- [x] Q-1: **1ライン 1枚・上限 4枚**（G-11）
- [x] Q-2: **スタッフ立会いなし。アプリ内で完結**（G-10）→ PIN・QR・`verified_by` は作らない

## 実装で必ずやる

- [ ] 換算を DB・通信に触れない純関数として切り出す（`src/lib/gacha/coins.ts`）
- [ ] `gacha_coin_uses` に `uk_gacha_coin` と `uk_gacha_idem` を張る
- [ ] 消費を単一 SQL（`HAVING COUNT(*) < earned`）にする
- [ ] 冪等キーをクライアント生成・必須にする
- [ ] `MAX_COINS` のハードコードを消し、`gacha_settings` に寄せる（既定 `1枚/ライン・上限4枚`）
- [ ] 誤操作防止を UI に入れる（押下即 disabled・完了画面に枚数目と残数・完了から戻れない）
- [ ] 当日ガチャを止められるようにする（`is_enabled`）
- [ ] `ApiParticipantClient` のガチャスタブを削除する

## テストで必ず確認する

- [ ] 同一冪等キーの並行2回で **行1件・枚数1枚**（C-1）
- [ ] 残高1枚に別キー並行2回で **成功1本**（C-3）
- [ ] 残高5枚に別キー並行5回で `coin_index` に**重複も欠番も無い**（C-4）
- [ ] 残高0での使用が `409`、台帳が増えない（C-5）
- [ ] `used > earned` になる行がどのケースでも生まれない
- [ ] `src/lib/bingo/` が `gacha` を一切参照していない
- [ ] 純関数 264 通りの総当たり
- [ ] 実機リハーサルで「素早く2回タップ」と「機内モード後の再試行」を必ず実施
- [ ] **参加者ひとりで最後まで進めること**をリハーサルで確認（スタッフの助けを借りない）

## 当日に備える

- [ ] 運営が `is_enabled` / `bonus_coins` を変更する手順書を用意する
- [ ] コイン使用状況を当日リアルタイムで見られる（admin stats）
- [ ] 誤使用の救済は `bonus_coins` +1 で行うと運営に周知する（**台帳から行を消さない**）
- [ ] 本番相当データ量での負荷確認（ピーク: 1分あたり9.3チェックイン・同時滞在89名）
