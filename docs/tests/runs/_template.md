# テスト実行記録 — YYYY-MM-DD（概要を一行で）

## 何を

### 対象（src）

- `src/...`（テスト対象のソースファイル）

### テストコード（tests）

- `tests/unit/....test.ts`（追加・変更したテストファイル）

## なぜ

（Issue / ADR / 不具合との対応。なぜ今このテストをしたか）

## 実行コマンド

```bash
npm test
npm run build
```

## 環境

- ブランチ: `branch-name`
- MySQL: 起動済み / 未使用
- 関連 PR / Issue: #NNN（あれば）

## 結果

- （成功 / 失敗の要約）
- （失敗時はログ抜粋・再現手順）

## メモ

（次回に引き継ぐこと、未解決の点）
